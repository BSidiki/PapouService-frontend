import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.papouservice.app',
  appName: 'PapouService',
  webDir: 'dist/ps-frontend/browser'
};

export default config;
